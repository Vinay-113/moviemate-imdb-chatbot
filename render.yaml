services:
  - type: web
    name: moviemate-imdb-chatbot
    runtime: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/insights
    autoDeploy: true
